{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "FabLabs ${libname} version ${libver}",
  "icon": "assets/blueprint/icon.png",
  "description": "Style builder",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "*",
  "mixins": [
    "mixins.${libname}.json"
  ],
  "entrypoints": {
    "main": [
      "io.github.fablabsmc.fablabs.impl.stylebuilder.user.StyleBuilderUser::init"
    ]
  },
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  }
}
